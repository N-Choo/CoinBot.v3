services:
  backend-dev:
    build:
      context: .
      dockerfile: api_gateway/Dockerfile
      target: dev-builder
    command: cargo run -p api-gateway
    env_file:
      - .env
    ports:
      - "8081:8080"
    volumes:
      - .:/app
      - /app/target
    environment:
      - RUST_LOG=debug
    networks:
      - app-network

  backend:
    build:
      context: .
      dockerfile: api_gateway/Dockerfile
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - .:/app
      - /app/target
    environment:
      - RUST_LOG=debug
    networks:
      - app-network

  frontend:
    image: node:22-alpine
    working_dir: /app
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
    ports:
      - "5173:5173"
    volumes:
      - ./react:/app:Z
      - /app/node_modules
    depends_on:
      - backend
    networks:
      - app-network
    environment:
      - VITE_API_URL=http://localhost:8080

  frontend-prod:
    profiles:
      - prod
    build:
      context: ./react
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
