[package]
name = "common"
version = "0.1.0"
edition = "2021"

[dependencies]
tonic = "0.12"
prost = "0.13"
serde = { version = "1", features = ["derive"] }
thiserror.workspace = true

[build-dependencies]
tonic-build = "0.12"
